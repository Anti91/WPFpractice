<UserControl x:Class="WPFsumApp.View.LoginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFsumApp.View"
             xmlns:user="clr-namespace:WPFsumApp.Model"
             mc:Ignorable="d" Name="UserControl"
             d:DesignHeight="75" d:DesignWidth="120">
    
    <Border BorderBrush="Black" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Content="Username:" Grid.Row="0" Grid.Column="0"/>
            <ComboBox Grid.Row="1" Grid.Column="0" Margin="3"  ItemsSource="{Binding MyProperty, RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Name"/>
            <Button Content="Login" Grid.Row="2" Grid.Column="0" Margin="22,3,0,3" />
        </Grid>
    </Border>
</UserControl>
